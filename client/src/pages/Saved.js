import React from "react";

const Saved = () => {
  return (
    <div className="">
      {/* {loggedIn && artist ? (
        <VenueCard venuesData={artist.saved}></VenueCard>
      ) : null}
      {loggedIn && host ? (
        <ArtistCard artistsData={host.saved}></ArtistCard>
      ) : null} */}
    </div>
  );
};
export default Saved;
